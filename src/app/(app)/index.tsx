import { ReqImageHandler } from "@/src/features/satellite-images/components/req-images-handler";
import { Text, View } from "react-native";

export default function NewRequest() {
  return (
      <ReqImageHandler />
  );
}
